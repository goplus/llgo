- Parallel host demo CI (LLGO_DEMO_JOBS=4) caused _demo/go/sync segfault on ubuntu Go 1.21; investigate demo/build-cache concurrency before re-enabling in CI.
- Consider parallelizing test/buildcache modes (native/wasm/esp32c3) with isolated XDG_CACHE_HOME/LLGO cache roots so cache-clearing tests do not interfere; validate carefully before enabling in CI.
- Build-cache workflow now caches the iwasm binary at the llgo cache path keyed by WAMR version/dev/build_iwasm.sh and skips WAMR clone/cmake/make on cache hits; latest monitored run completed both build-cache jobs successfully with max 169s. The LLGo cross-compile workflow now uses the same cache/script pattern; first monitored run built/saved iwasm and completed cross-compile in 101s with zero CI failures.
- Latest full larger-retry PR run completed with zero failures/pending. Build-cache max 178s and Go split jobs had no over-15 jobs; one macOS startup smoke outlier hit 16m02s, but recent history was only 1/6 over 15m (962s, 638s, 660s, 546s, 612s, 576s), so do not split further based on that single outlier.
- Weighted macOS llgo test sharding based on one run did not improve live max shard time (12m59s vs prior 12m58s) and worsened spread; avoid hard-coded shard weights unless backed by more runs or a dynamic historical dataset.
- Analyze macOS targetsbuild (~10m Build targets step) for duplicate setup/build work between empty and defer modes; optimize only if target coverage remains equivalent.
- New target: pure compile / `go build` efficiency, not CI workflow fan-out. Baseline clean `go build -a -tags=dev ./cmd/llgo` median was 10594ms. Kept source optimizations: compact huge `newlibesp.go` and `compiler_rt.go` file lists from many `filepath.Join` expressions into raw string lists, improving median to 10301ms. Tried and rejected: applying same refactor to smaller `picolibc.go`; moving lists to `go:embed`; hard-coding `go build -p` (environment-specific and not better than source change).
- For future compile-speed work, investigate dependency/package-graph reductions and LLGo's own build pipeline parallelism. `buildAllPkgs` is sequential today; any parallelization must be dependency-aware and guarded because LLVM/C ABI transformer/package generation may not be thread-safe.

- Current target: pure compile / `go build` and LLGo build-pipeline efficiency. Do not optimize by CI workflow fan-out, do not change YAML cache manifest format, and avoid using CI as the first timing experiment. For local `cmd/llgo` build checks, use `go build -o <tmp> -tags=dev ./cmd/llgo` or ensure root `/llgo` stays ignored; the root `llgo` executable was accidentally tracked once and has been removed.
- Archived/out-of-scope CI notes: broad host-demo parallelism (`LLGO_DEMO_JOBS=4`) caused `_demo/go/sync` segfault on Ubuntu Go 1.21; weighted macOS llgo sharding based on one run did not help; do not split further based on single macOS startup-smoke outliers; build-cache/iwasm caching and download retry work are already implemented. Revisit CI-only ideas only if the optimization target explicitly changes back to CI wall time. A later macOS Intel esp32 embedded job failed in Install embedded dependencies due repeated Espressif QEMU `curl: (22) 502` downloads; unrelated to compile changes, but if CI resilience becomes target, consider stronger QEMU download retry/backoff, mirror, or cache handling.
- New target baseline: clean `go build -a -tags=dev ./cmd/llgo` median was 10594ms. Kept source optimizations: compact huge `newlibesp.go` and `compiler_rt.go` file lists from many `filepath.Join` expressions into raw string lists, improving median to 10301ms. Tried and rejected: applying same refactor to smaller `picolibc.go`; moving lists to `go:embed`; hard-coding `go build -p` (environment-specific and not better than source change).
- For future compile-speed work, investigate dependency/package-graph reductions and LLGo's own build pipeline parallelism. Kept fingerprint/cache-stage cleanups: `collectFingerprint` now hashes the manifest string it already built instead of calling `m.Fingerprint()` and rebuilding YAML; synthetic benchmark improved from 126488ns/op and 1311 allocs/op to 69680ns/op and 676 allocs/op. `saveToCache` now skips decode/re-marshal when metadata is empty and writes the already-built manifest unchanged; synthetic empty-metadata benchmark improved from 314572ns/op and 247 allocs/op to 295421ns/op and 42 allocs/op. `parseManifestMetadata` now avoids YAML unmarshal for modern manifests with no top-level metadata section while preserving YAML-with-metadata and legacy INI parsing; synthetic no-metadata benchmark improved from 50702ns/op and 905 allocs/op to 714ns/op and 1 alloc/op. For modern manifests with metadata, it now unmarshals only the top-level metadata section; synthetic metadata benchmark improved from 64467ns/op and 973 allocs/op to 7831ns/op and 104 allocs/op. Simple generated YAML metadata blocks containing only `need_rt`/`need_py_init` now use a narrow parser; benchmark improved 3130ns/op, 8104 B/op, 65 allocs/op to 138ns/op, 64 B/op, 2 allocs/op. The parser now also handles simple unquoted generated `link_args` list items and falls back to YAML for quoted/complex scalars; link_args metadata improved 4852ns/op, 9687 B/op, 105 allocs/op to 279ns/op, 128 B/op, 3 allocs/op. Allowing comma in simple scalars keeps common `-Wl,...` args on the fast path; comma link_args improved 4269ns/op, 9295 B/op, 96 allocs/op to 190ns/op, 128 B/op, 3 allocs/op. Replacing the scalar guard's `strings.ContainsAny` with a manual byte switch was rejected as too small/readability-negative despite parse 189ns->180ns and append 118ns->104ns with unchanged allocations. Delaying the unused empty metadata allocation in that path was rejected as too small despite 134ns->129ns and 2->1 allocs. `digestFilesWithOverlay` now skips `sort.Slice` for zero/one-file lists; synthetic single-file benchmark improved from 5368ns/op and 4 allocs/op to 1382ns/op and 3 allocs/op. Replacing its multi-file `sort.Slice` with `slices.SortFunc` was rejected because the eight-file benchmark only moved 10849ns->10501ns (~0.35µs) despite fewer allocs. Adding a `len(overlay)==0` branch to skip per-file nil overlay map lookups in `digestFilesWithOverlay` was rejected because the single-file no-overlay benchmark regressed/noised (1159ns->1171ns, unchanged allocs). The analogous `collectDependencyInputs` single-import sort guard was rejected despite a 104ns->71ns microbenchmark change because the absolute gain is too small for real build wall time. Returning the original slice from `sortDeps` for a single dependency was also rejected (7425ns->6854ns) because the absolute gain was ~0.57µs and introduced avoidable aliasing assumptions. Hoisting the `collectEnvInputs` env-var list was rejected (734ns->730ns, unchanged allocs), but caching the full per-build env manifest section on `context` was kept: collectEnvInputs improved 608ns/op, 1184 B/op, 7 allocs/op to 13ns/op, 0 B/op, 0 allocs/op, and collectEnvInputs+Common+Build improved 10210ns/op to 9503ns/op. Formatting `AbiMode` with `strconv.Itoa` instead of `fmt.Sprintf` in `collectCommonInputs` was rejected as too small (95ns->65ns, unchanged allocs). Kept: cache the sorted build-tag slice on `context` so per-package `collectCommonInputs` no longer repeats `strings.Split`/sort; tag collection improved 79ns/op, 64 B/op, 1 alloc/op to 36ns/op, 0 B/op, 0 alloc/op, while collect+Build moved 9399ns/op to 9278ns/op. Directly assigning crosscompile flag slices instead of copying them was rejected despite improving collectCommonInputs flags-only 85ns->36ns and 3->0 allocs, because collect+Build did not improve (15298ns->15363ns) and aliasing immutable config slices was not worth the risk. Caching the entire `commonSection` was also rejected despite improving collectCommonInputsFull 85ns->16ns and 3->0 allocs because the fuller collectEnv+Common+Build workload regressed/noised 14773ns->14991ns and introduced shared-section aliasing assumptions. Guarding manifest build-tag sorting with `sort.StringsAreSorted` was rejected because sorted-tag Build regressed/noised 5487ns->5564ns (unsorted 5452ns->5515ns). Pointing `manifestData` at `manifestBuilder` fields directly instead of local section copies was rejected as too small despite 8642ns->8543ns and 121->118 allocs because it changes Build mutation semantics for only ~0.1µs. Replacing `newManifestBuilder()` in `collectFingerprint` with a stack local was rejected because an empty-package collectFingerprint benchmark regressed/noised 9601ns->9732ns with unchanged allocations. Caching `cacheEnabled()` on context was not pursued because a baseline showed it is already only ~11ns/op and `tryLoadFromCache` without fingerprint is ~12ns/op with zero allocations. Avoiding the empty `p.OtherFiles` copy in `collectPackageInputs` was rejected (30717ns->30287ns, unchanged allocs). The `pkgSFiles` no-assembly fast path now uses one `os.ReadDir` scan instead of two `filepath.Glob` calls; the common no-.s/.S `collectPackageInputs` benchmark improved from 31549ns/op, 4048 B/op, 25 allocs/op to 14089ns/op, 1840 B/op, 12 allocs/op with tests for empty/no-asm, .s, .S, and missing-dir fallback. Nil no-asm results are cached so collectFingerprint + compilePkgSFiles cache-miss paths do not repeat the scan; a two-lookup benchmark improved from 23459ns/op, 2912 B/op, 18 allocs/op to 12378ns/op, 1856 B/op, 11 allocs/op, and a cache-entry test was added. `dirHasAsmFile` then switched from sorted `os.ReadDir` to unsorted `File.Readdirnames`; direct 24-file no-asm scan improved from 18121ns/op, 4296 B/op, 60 allocs/op to 17687ns/op, 2760 B/op, 36 allocs/op, and the collectPackageInputs no-asm check stayed improved at 13075ns/op. Replacing its `strings.HasSuffix` checks with a manual two-byte suffix check was rejected (17265ns->17698ns, unchanged allocs). For non-empty cache metadata, `saveToCache` now marshals only the metadata block and inserts it into the already-built manifest, improving the synthetic metadata save benchmark from 428810ns/op and 1371 allocs/op to 317801ns/op and 102 allocs/op while preserving parsed metadata and metadata-before-deps placement. Simple generated metadata now also uses a manual YAML emitter with fallback for complex link args, improving appendManifestMetadata from 3693ns/op, 17349 B/op, 56 allocs/op to 146ns/op, 368 B/op, 2 allocs/op; a follow-up builds the final manifest directly for simple metadata and improves further to 121ns/op, 256 B/op, 1 alloc/op while preserving metadata-before-deps and no-trailing-newline behavior. Removing the defensive `pkg.LinkArgs` copy in saveToCache was rejected because it saved one allocation/32B but worsened/noised the file-I/O-dominated save benchmark 294400ns->297562ns. Splitting no-Mkdir variants for copy/write after `EnsureDir` was rejected because saveToCache only moved 294094ns->293454ns (~0.2%) despite allocs 40->36, not enough for added helper complexity. `fingerprintManifest` now avoids copying the manifest string before SHA-256 by using a read-only unsafe byte view; a ~23KB manifest benchmark improved from 9156ns/op and 24704 B/op to 7773ns/op and 128 B/op with an equivalence test against sha256. `readManifest` now also avoids copying os.ReadFile's fresh byte buffer when converting to manifest text; the same ~23KB read benchmark improved from 15039ns/op and 49577 B/op to 13352ns/op and 25000 B/op. A follow-up zero-copy `yaml.Marshal` []byte-to-string conversion was rejected as too small/risky (331975ns->330859ns, one alloc saved). Hoisting `filepath.Clean(cm.root)` out of `cacheManager.packageDir` was rejected because PackagePaths regressed/noised (728ns->740ns, unchanged allocs); re-evaluating after later PackagePaths optimizations was still rejected as too small (235ns->226ns, unchanged allocs). `context.targetTriple` now caches the computed target triple string, avoiding repeated fmt/concat work on cache load/save paths; host triple lookup improved from 62ns/op, 48 B/op, 3 allocs/op to ~1ns/op, 0 B/op, 0 allocs/op. The uncached host triple helper now uses direct string concatenation instead of `fmt.Sprintf`, improving from 57ns/op, 48 B/op, 3 allocs/op to 10ns/op, 0 B/op, 0 allocs/op. `sanitizePkgPath` now has a safe slash-separated import-path fast path that avoids `strings.Split`/`filepath.Join` allocations while preserving fallback sanitization; `PackagePaths` for a safe package path improved from 644ns/op, 672 B/op, 7 allocs/op to 542ns/op, 544 B/op, 5 allocs/op. Removing a redundant `filepath.Clean` immediately after `filepath.Join` in `packageDir` further improved the same benchmark from 547ns/op to 493ns/op. `packageDir` now also concatenates sanitized root/target/pkg directory components directly while preserving filepath.Join-equivalent root edge cases; PackagePaths improved from 232ns/op to 189ns/op with unchanged allocations. `PackagePaths` now builds archive/manifest paths via direct string concatenation after computing the cleaned directory instead of calling `filepath.Join` twice; the benchmark improved from 490ns/op, 544 B/op, 5 allocs/op to 241ns/op, 384 B/op, 3 allocs/op. Reusing an intermediate `dir+separator+fingerprint` prefix was rejected because it added an allocation and regressed (234ns->251ns, 384 B/op->528 B/op). Replacing the packageDir root+separator `strings.HasPrefix` guard with an indexed `os.IsPathSeparator` helper was rejected (506ns->544ns, unchanged allocs). A 64-hex fingerprint filename fast path was rejected as too small (540ns->530ns, unchanged allocs). A mutable `cacheManager` package-directory map was rejected because a repeated PackagePaths benchmark only moved 470ns->449ns while allocations worsened 6->9 and bytes 768->1280, plus it complicates future parallelism. Changing `isSafeComponent` from rune iteration to byte iteration was rejected as too small and semantically noisy (234ns->221ns, unchanged allocs). Using `readOnlyStringBytes` when hashing rare unsafe cache path components was rejected despite a microbenchmark win (68ns->57ns, allocs 3->2) because the path is rare and broadens unsafe usage. A follow-up that merely deferred `manifestMetadata` allocation in the empty save path saved one allocation but only moved wall time by ~0.1%, so it was rejected as noise-level churn. `buildAllPkgs` is sequential today; any package-level parallelization must be dependency-aware and guarded because LLVM/C ABI transformer/package generation may not be thread-safe. Safer external-process parallelism was implemented in `CompileGroup`: independent C/ASM files now compile concurrently (default `min(GOMAXPROCS,16)`, override `LLGO_COMPILE_JOBS`, per-file command logs opt-in via `LLGO_COMPILE_VERBOSE`). A 64-file local compile-group benchmark improved from 905ms sequential to 173ms default parallel; a 96-file tuning run improved from 241.5ms at 8 jobs to 164.8ms with the cap raised to 16. `CompileConfig.Compile` now shares one worker pool across archive groups and archives deterministically afterward; a synthetic 4-group/96-file benchmark improved from 233.5ms to 189.5ms while crosscompile tests and clean cmd/llgo go-build checks passed. Independent archive creation now also runs in parallel after compilation; an 8-group/128-file benchmark improved from 257.9ms to 215.7ms. Multi-group worker compiler lookup now uses a dense slice instead of a map; the 8-group/128-file benchmark improved from 222.4ms to 219.2ms, with a 9-run confirmation median of 215.8ms. The archive phase now precomputes the `llvm-ar` path once; the 8-group/128-file benchmark improved from 218.2ms to 213.3ms, with a 9-run confirmation median of 209.7ms. CompileGroup object files now use deterministic per-index paths inside the isolated temp dir instead of one `os.CreateTemp` per source; a 96-file benchmark improved from 167.1ms to 158.0ms. Simple `fmt` usage was removed from the CompileGroup hot package; a 96-file benchmark improved from 162.9ms to 159.7ms and clean cmd/llgo go-build remained in the recent-noise band (10430.6ms median). The common single-group parallel worker path was specialized to avoid per-task group-map lookups; a 128-file benchmark improved from 225.5ms to 208.8ms, with a 3-run confirmation median of 213.4ms. A direct single-group CompileGroup refactor was tested and rejected (216.1ms to 220.0ms). Changing archive mode from `llvm-ar rcs` to `qcs` was also rejected: isolated archive microbenchmark improved only ~1.5%, while the 8-group CompileConfig workload was slightly worse (209.7ms rcs/precomputed baseline vs 210.8ms qcs) and archive semantics churn was not justified. Preallocating the internal compile-task slice was also rejected (no allocation win visible in subprocess-dominated 8-group/128-file benchmark). Tracking the active group to avoid the `singleTaskGroup` scan was rejected too (128-file median 208.4ms baseline vs 211.8ms changed). Preallocating `archiveObjects` argv capacity showed only a 0.5ms/noise-level delta on the 8-group/128-file benchmark and was rejected as non-obvious code churn. Simple crosscompile archive-name `fmt.Sprintf` calls were replaced with string concatenation; a noisy clean go-build confirmation showed 10911.7ms baseline vs 10551.5/10573.8ms post-change, but earlier unrelated samples were ~10341ms so treat this as a small safe cleanup. Newlib ESP `joinFileList` now cleans the root once and uses prefix concatenation instead of per-entry `filepath.Join`, improving RISCV config construction from 112251ns/op to ~56504ns/op while preserving filepath.Join-equivalent output in tests. Reusing that helper for Picolibc's 112-file list improved config construction from 9085ns/op to 5686ns/op with an exact file-count test. The same optimization in compiler-rt improved RISCV config construction from 12787ns/op to 6339ns/op with equivalent-output tests. Replacing newlib `strings.Fields` with a custom ASCII scanner was rejected: allocs dropped slightly (1424->1418) but wall time regressed (58607ns->65131ns). Sequential `clang.Cmd` wrapper reuse was tested and rejected (1316.6ms to 1350.2ms). `parseCgoPreamble` now writes `#line` directives with direct `strconv` formatting instead of `fmt.Sprintf`, improving a representative benchmark from 853ns/op, 1313 B/op, 30 allocs/op to 799ns/op, 1288 B/op, 29 allocs/op with a quoted-filename test. It then switched from `strings.Split` to manual line scanning while preserving trailing-empty-line behavior, improving the same benchmark further from 780ns/op, 1288 B/op, 29 allocs/op to 749ns/op, 1208 B/op, 28 allocs/op. Pre-growing the preamble builder then improved it further from 750ns/op, 1208 B/op, 28 allocs/op to 673ns/op, 944 B/op, 23 allocs/op. Replacing `strings.SplitN` with `strings.IndexByte` inside `parseCgoDecl` was rejected as too small despite 161ns->153ns and one alloc saved. Optimizing `cachedPkgConfig` hit key construction was not pursued because the measured cost (383ns/op, 2096 B/op, 4 allocs/op) mostly comes from preserving exact environment-sensitive semantics via `os.Environ`; caching or narrowing PKG_CONFIG env handling would change semantics. `collectCgoSymbols` now parses fixed-format generated `_cgo_hash_(Cfunc|C2func|Cmacro)_name` symbols manually instead of using a regexp, improving a representative benchmark from 1900ns/op, 1881 B/op, 20 allocs/op to 634ns/op, 984 B/op, 6 allocs/op; tests cover malformed names and the Cmalloc fix. A follow-up switched parseCgoExternSymbol kind matching from built marker strings to constant prefix checks, improving the same benchmark from 628ns/op to 536ns/op with unchanged allocations. `parseCgo_` now scans package-local `.c` files with `Readdirnames` instead of `filepath.Glob`, preserving sorted output for actual C files and skipping `_test.c`; the common no-cgo single-dir benchmark improved from 13178ns/op, 2008 B/op, 11 allocs/op to a confirmed 11615ns/op, 1368 B/op, 6 allocs/op. A single-dir dirs-map hoist on top was tried and not kept because it did not improve the benchmark.
- Rejected LLGo runtime compile micro-optimization: caching `extraAsmSigsAndDeclMap(pkgPath, goarch)` once per plan9asm translation instead of rebuilding it inside the ManualSig callback. Initial `go test` wall-time benchmark was noisy/worse (1184.8ms baseline vs 1391.0ms after edit), and a later isolated in-package benchmark also regressed (5.651ms/op to 5.802ms/op). Do not revisit unless allocation profiles show this function is material.
- Rejected command dependency experiment: resolving `llgo monitor -target` serial ports directly via `internal/targets` instead of `crosscompile.UseTarget` passed helper tests but clean cmd/llgo go-build regressed (10647.2ms to 10678.2ms) and subtly changed monitor target validation/side effects. Revisit only for a monitor-startup behavior target, not current pure go-build efficiency.
